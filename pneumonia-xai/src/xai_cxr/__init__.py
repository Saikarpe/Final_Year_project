"""xai_cxr — explainable chest X-ray pneumonia classification.

Everything the app and the scripts under scripts/ import lives here, so there
is exactly one implementation of preprocessing, the model architecture, each
explanation method, the conformal predictor, and every evaluation metric.
See CLAUDE.md for the hard rules this package exists to enforce.
"""

__version__ = "0.2.0"
